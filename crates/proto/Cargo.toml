[package]
name = "proto"
version = "0.0.0"
edition = "2024"

[dependencies]
hittest = { path = "../hittest" }
mlua = { version = "0.11.6", features = ["lua54", "vendored"] }

[[bin]]
name = "proto"
path = "src/main.rs"
