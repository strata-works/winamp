import XCTest

final class ShowcaseTests: XCTestCase {
    func testPlaceholder() {
        // Placeholder to keep the ShowcaseTests target buildable until real tests are added.
        XCTAssertTrue(true)
    }
}
